<div id="Gmap"
     style="width:500px; height:500px"></div>

<script src="https://maps.googleapis.com/maps/api/js?key=@ViewBag.GoogleMapsApiKey&callback=InitMap&libraries=marker&v=weekly" defer></script>
<script>
    var Marker;

    console.log("VC Default");

    function InitMap(){
        //console.log("Initing GoogleMap");
        print("Initing GoogleMap");

         // 建立地圖
        const map = new google.maps.Map(document.getElementById("Gmap"), {
            center: { lat:@ViewBag.MapCenterLatitude, lng: @ViewBag.MapCenterLongitude },//設定地圖中心點
            zoom: 10,
            mapId: "DEMO_MAP_ID"
        });

        // 監聽地圖的點擊事件
        // map.addListener("click", (Event) => {
        //     const Lat = Event.latLng.lat();
        //     const Lng = Event.latLng.lng();

        //     // 將座標填入表單欄位
        //     document.querySelector("input[name='Latitude']").value = Lat;
        //     document.querySelector("input[name='Longitude']").value = Lng;

        //     //移除地圖上舊有Marker
        //     if (Marker) {
        //         Marker.setMap(null);
        //     }

        //     //在地圖上建立新Marker
        //     Marker = new google.maps.marker.AdvancedMarkerElement({
        //         position: { lat: Lat, lng: Lng },
        //         map: map,
        //         title: "新增到訪紀錄"
        //     });
        // });
    }

    window.InitMap = InitMap; // 將 InitMap 函數暴露給全域作用域，以便 Google Maps API 可以調用它
</script>